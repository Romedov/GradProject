<?xml version="1.0" encoding="UTF-8"?>
<Frame xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="Kassa.Views.UserControls.FrameButton"
       
    x:Name="FrameLayout">
    <!--<Frame.GestureRecognizers>
        <TapGestureRecognizer 
            x:Name="Tapper" 
            Command="{Binding Command}"
            CommandParameter="{Binding CommandParameter}"/>
    </Frame.GestureRecognizers>-->

    <FlexLayout x:Name="Flex" BackgroundColor="Transparent" Padding="0" Margin="0">
        <Image x:Name="ButtonImage" Margin="0,0,10,0" 
            HorizontalOptions="Start" Aspect="AspectFit"
            VerticalOptions="CenterAndExpand"/>
        <Label x:Name="ButtonLabel" 
            Margin="0,0,0,0" Padding="0,0,0,0" 
            VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"
            FlexLayout.Grow="1"/>
    </FlexLayout>
</Frame>